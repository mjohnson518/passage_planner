# Force npm usage and prevent yarn confusion
engine-strict=false
legacy-peer-deps=false
# Ensure npm registry is used
registry=https://registry.npmjs.org/
# Disable package-lock conflicts
package-lock=true

